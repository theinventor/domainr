<h1>Listing domains</h1>
<div>
  <%= form_tag search_domains_path, :method => "get" do %>
    <%= label_tag(nil, "Search for domains:") %>
    <%= text_field_tag "keyword" %>
    <%= submit_tag("Search") %>
  <% end %>
</div>
<table>
  <tr>
    <th><%= sortable "domain" %></th>
    <th>Registration date</th>
    <th>Expiration date</th>
    <th>Title Diff</th>
    <th>Last IP</th>
    <th></th>

  </tr>

<% @domains.each do |domain| %>
  <tr>
    <td><%= domain.domain %></td>
    <td><%= domain.registration_date %></td>
    <td><%= domain.expiration_date %></td>
    <td><%= domain.page_title_diff %></td>
    <td><%= domain.ip_address %></td>
    <td><%= link_to 'Remove', domain, confirm: 'Are you sure?', method: :delete %>
      <%= link_to "recheck", "/update_now/#{domain.id}" %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Domain', new_domain_path %>
